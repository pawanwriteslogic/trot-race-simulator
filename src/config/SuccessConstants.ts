export enum SuccessConstants {
  SUCCESSFUL_REQUEST = 'Successful request',
  SERVER_CONNECTED = 'Connected to the server on port',
  DATABASE_CONNECTED = 'Connected to the database',
  SUCCESSFUL_AUTHENTICATION = 'SUCCESSFUL AUTHENTICATION',
}
